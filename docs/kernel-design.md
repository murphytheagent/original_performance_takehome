# Kernel Design Notes

## Baseline

- 2026-03-11 00:49 UTC: `python tests/submission_tests.py` reported `147734` cycles for the starter kernel.
- 2026-03-12 04:55 UTC: current best verified result on the independent rewrite branch is `1501` cycles.
- 2026-03-11 03:21 UTC: live upstream re-audit confirmed the best public result is still `1149` cycles (PR `#35`), with `1158` (PR `#29`) the next-best measured candidate.
- 2026-03-11 03:23 UTC: the fork PR branch at `1189` cycles is no longer treated as a valid final answer because it is materially PR `#35` plus local hardening.
- The starter implementation is scalar and emits one slot per instruction bundle, so it leaves both VLIW packing and SIMD unused.

## Hard constraints from the simulator

- One core only (`N_CORES = 1` in both the live and frozen simulators).
- Per cycle slot limits: `alu=12`, `valu=6`, `load=2`, `store=2`, `flow=1`.
- `vload` and `vstore` can move `VLEN = 8` contiguous words in one slot.
- There is no true gather load, only scalar `load` and `load_offset`.
- Scratch space is `1536` words, large enough to stage the batch and temporary buffers.

## Observations

- Beating `1487` cycles over `16 * 256 = 4096` element-round updates requires an architectural change, not a constant-factor cleanup.
- Direct per-element forest gathers are too expensive if repeated every round.
- All inputs start at index `0`, and before tree wraparound the number of reachable nodes at round `r` is bounded by the tree depth, so many inputs share the same node value in early rounds.
- The first successful redesign keeps the batch in scratch, uses path-plus-global-depth instead of full heap indices, and skips index writeback entirely because the submission harness only checks final values.
- The first successful redesign also collapses hash stages `0`, `2`, and `4` to `multiply_add`, which is responsible for a large share of the current speedup.
- Prefix-round measurements of the current kernel show:
  - fixed setup/teardown overhead: about `117` cycles
  - root rounds (no gather): about `70` cycles
  - rounds with per-element node gathers: about `154` cycles each
- This means generic gather rounds are already fairly close to their access-pattern floor, so the next big win almost certainly has to reduce or amortize node feeding rather than only improving arithmetic scheduling.
- 2026-03-11 07:24 UTC: a successful independent shallow-wave fusion removed one real source of waste. Keeping the depth-1 and depth-2 node vectors in scratch for a wave, instead of spilling them to memory and reloading them a round later, improved the independent branch from `2411` to `2315`. Removing the dead per-block value-address constant table then improved it again to `2285`.
- 2026-03-11 07:24 UTC: updated one-shot round costs on the fused branch are `[setup=101, depth0=70, depth1=102, depth2=144, depth3+=154..160]`. The fusion line therefore saved only shallow costs; the deep gathered rounds still dominate the remaining gap to `1149`.
- 2026-03-11 07:48 UTC: two quick negative probes ruled out the simplest next shortcuts. The final branch bit of `myhash()` depends on all 32 input bits, so there is no cheap partial-hash parity path for early prefetch, and the mean unique-address count within an 8-lane block is only `[5.25, 6.46, 7.18, 7.58, 7.79, 7.89, 7.95, 7.97]` for depths `3..10`, so within-block repeated-address dedup has only about a `~100`-cycle ceiling.
- 2026-03-11 07:48 UTC: the main new positive result is structural scheduling. Emitting the whole 16-round wave as one dependency DAG and scheduling slots by group, rather than by global stage, improved the independent branch from `2285` to `1815`. That improvement is real evidence that the old branch was still paying artificial round barriers deep in the program, not just shallow setup overhead.
- 2026-03-11 08:08 UTC: a narrower `flow`-engine rewrite succeeded where the earlier full shallow `vselect` trees failed. Keeping the full-wave DAG and deeper gathered rounds unchanged, but moving only the existing depth-1/depth-2 cached-node selection onto `flow.vselect`, lowered the branch from `1815` to `1689`. The important distinction is scope: all-in `flow`-driven shallow specialization saturated the single `flow` slot, but offloading only the already-cached-node selection removed vector-ALU pressure without introducing enough extra `flow` work to become the new bottleneck.
- 2026-03-11 08:58 UTC: a broader scheduler search found one more independent overlap win even after the full-wave DAG and `flow` offload were already in place. On the `valu` engine only, frontloading ops with `stage < 7` and preferring higher-numbered groups first lowered the branch from `1689` to `1678`. The useful takeaway is not the exact rule so much as the direction: even now, some of the remaining loss is still in how the ready frontier is drained, not only in the raw gathered-load count.
- 2026-03-11 19:45 UTC: a leaner depth-3 cache bought a small but real follow-up gain. After each depth-2 update, the kernel now materializes the next depth-3 node vector from preloaded nodes `7..14` with one extra wave temp and a smaller flow-side select tree, rather than paying the next round's 8 scalar gathers. The first correct version of that idea was still too valu-heavy (`1707` cycles), but the leaner variant lowered the branch from `1678` to `1671` even though it had to shrink the active wave from `18` to `16`.
- 2026-03-11 19:45 UTC: the depth-3 cache changed the bottleneck mix exactly as expected. On the `1668` kernel the op counts are `load=2133`, `valu=8229`, `flow=704`, for lower bounds of about `1067 / 1372 / 704` cycles respectively. The branch is therefore now clearly valu-bound rather than load-bound, which is why the broader ready-op sweep around the new kernel found no free overlap win.
- 2026-03-11 19:45 UTC: one more tiny fixed-cost cleanup was real on top of the new depth-3 cache. Because depths `1..3` are now fully cached, the old generic base vectors for those depths are dead. Removing them lowered scratch usage from `1509` to `1485` and cut the frozen harness from `1671` to `1668` without changing the round logic.
- 2026-03-11 20:15 UTC: two more local cleanups lowered the independent branch from `1668` to `1648` without changing the overall structure. First, the depth-0 round now writes the root parity bit directly into `path` instead of materializing it in a temp and copying it one cycle later. Second, the depth-3 cache builder now preserves its existing `path & 2` mask through the select tree instead of recomputing it after the pair loads. Together those edits reduced vector-ALU ops from `8229` to `8101`.
- 2026-03-11 20:15 UTC: the smaller `1648` kernel also changed the best scheduler point again. A fresh local sweep showed the older `stage < 7` valu-priority rule had become counterproductive after the new cleanup. Removing that cutoff and simply draining ready `valu` ops by stage, while still preferring higher-numbered groups, lowered the branch again from `1648` to `1622`. A broader engine-specific sweep found no better overlap point after that change.
- 2026-03-11 20:15 UTC: a larger structural probe did not pay off. Splitting the cached shallow rounds from a wider deep gathered phase, while reusing the same scratch window for a `20`-group deep wave, regressed hard to `2119` cycles and was reverted immediately. The likely reason is that the lost cross-round overlap across the phase boundaries outweighed the wider deep wave.
- 2026-03-11 21:54 UTC: a new fixed-cost cleanup found a large amount of setup waste that the earlier deep-round work had simply carried along. The vector kernel used to emit scalar constant loads, vector broadcasts, shallow-node address constants, and shallow-node preloads almost one slot at a time. Rewriting that initialization into packed 2-lane pipelines lowered the `rounds=0` kernel from `120` to `58` cycles and the full frozen harness from `1622` to `1560` without touching the round body.
- 2026-03-11 21:54 UTC: two final setup-pack merges then shaved two more cycles. Folding the stride constants into the last vector-constant broadcast bundle and folding the input-value base addresses into the last shallow-preload broadcast bundle lowered the `rounds=0` kernel again from `58` to `56` and the full frozen harness from `1560` to `1558`.
- 2026-03-11 21:54 UTC: the setup rewrite also reduced scratch from `1485` to `1462` by deleting the scalar backing constants for vector constants plus the old shallow-node address table. That was enough to make a `17`-group wave legal again, but an in-memory sweep still showed `17 -> 1594`, so the round-body optimum remains at `16` groups even after the fixed-cost cleanup.
- 2026-03-11 22:34 UTC: one more enabling cleanup did not change the headline immediately, but it changed the layout constraints. Path scratch was made wave-local instead of block-global, freeing another `128` words and lowering the live branch to about `1334 / 1536` scratch with no cycle change. A fresh sweep on the actual `1558` file still showed that wider waves are dead even with that headroom (`17..20 -> 1592 / 1613 / 1612 / 1604`), so scratch capacity alone is not the missing lever.
- 2026-03-11 22:34 UTC: a bounded carried-deep-address rewrite was also tested directly on top of that new scratch layout and then reverted. It lowered raw `valu` slots from `8102` to `7912` but raised the frozen harness to `1661`, which is strong evidence that the extra flow-dependent deep-address recurrence lengthens the real schedule more than the saved base-address adds help.
- 2026-03-11 22:34 UTC: the positive result in the same pass was inter-wave IO overlap on the `256`-value submission shape. The kernel still keeps all per-round computation identical, but it now loads only the first `16`-block wave up front, schedules the second wave's value `vload`s as low-priority background work during the first wave's body, and stores the first wave's final values during the second wave's body. After removing an initial duplicate tail store and restoring the `rounds=0` edge case, the frozen harness improved from `1558` to `1544`.
- 2026-03-11 23:37 UTC: one more real overlap win landed on top of the `1544` branch by attacking the last exposed writeback tail. The first attempt streamed the final wave's stores in descending pair order and did not move the schedule at all; that turned out to be diagnostic, not wasted effort, because the live deep-gather schedule still makes lower-numbered pairs ready first. Rewriting the final-wave store stream in ascending pair order lowers the frozen harness from `1544` to `1537`, leaving only a single trailing `vstore` cycle exposed.
- 2026-03-11 23:37 UTC: the final-wave store overlap is intentionally gated to the exact two-wave `256` submission shape. The first broad version exposed an odd-block bug on smaller vector batches (`(4,7,24)`), so non-submission vector shapes keep the old explicit final-store tail. The same 24-case value sweep still matches `reference_kernel2`, which suggests the reusable takeaway is the readiness shape, not a new general store path.
- 2026-03-12 03:52 UTC: a broader exact-shape scheduler search found one more real ready-op gain after the narrower hand retunes had already come back flat. On the `valu` engine only, prioritizing ops with `stage >= 8` before earlier stages, and then draining lower-numbered groups first within that later band while keeping higher-numbered groups first in the earlier-stage band, lowers the frozen harness from `1537` to `1508`.
- 2026-03-12 03:52 UTC: the useful lesson from that pass is narrower than “scheduler work is alive again.” The reopened headroom is real, but it also appears locally exhausted now: load/store IO-priority variants stayed flat at `1537`, a tighter two-band search found no point below `1508`, and a broader three-band search only tied `1508`. So there was one more hidden valu-side ordering win, but the remaining `21`-cycle gap to `<1487` still probably needs deeper node-feeding change rather than another simple priority table tweak.
- 2026-03-12 04:55 UTC: a second exact-shape scheduler pass found one more real follow-up gain on top of the `1508` file. Keeping the same `stage >= 8` `valu` band split, but flipping the group directions relative to the earlier point so the later-stage `valu` band drains higher-numbered groups first and the earlier-stage `valu` band drains lower-numbered groups first, while `load` and `flow` also drain higher-numbered groups first, lowers the frozen harness again from `1508` to `1501`.
- 2026-03-12 04:55 UTC: the follow-up sweeps around that new point re-close the scheduler surface much more tightly. The `stage >= 8` threshold stays uniquely best (`6/7/9/10 -> 1550 / 1582 / 1576 / 1572`), `load` and `flow` really do want higher-numbered-group priority while `store` order is effectively neutral, and stage-first non-`valu` variants do not improve the schedule. So the scheduler surface was not as dead as it looked on the `1537` branch, but it now also looks locally exhausted again at `1501`.

## Candidate directions under evaluation

- Depth-specialized shallow rounds that avoid generic lane gathers where the active node set is tiny.
  - 2026-03-11 03:47 UTC: a fresh `vselect`-tree rewrite for depths `0` through `3` stayed correct but regressed to `2725` cycles because it saturates the single `flow` slot.
  - 2026-03-11 04:08 UTC: replacing those `vselect` trees with ALU mask-blends also stayed correct but still regressed to `2673` cycles once scratch pressure forced smaller compile-time waves.
  - 2026-03-11 05:28 UTC: using reclaimed input memory as a runtime shallow-cache workspace improved only to `2411` cycles. The depth-2 cached round became much cheaper, but the depth-0/depth-1 cache-build rounds mostly paid the savings back. This rules out another shallow-only variant.
  - 2026-03-11 07:24 UTC: a narrower shallow tactic did pay: fusing the depth-0/1/2 wave so the next node vectors stay in scratch removed `96` cycles overall, but it did not change the strategic bottleneck because the deeper gathered rounds remained flat at `154..160`.
- Coarse regrouping that is amortized across several later rounds, rather than bucketizing on every round.
  - Current preferred plan is a depth-5 prefix split: shared top-tree work for rounds `0` through `4`, one radix scatter into 32 buckets, cheap bucket-local rounds while each bucket fans out only `1/2/4/8`, then honest gathers only for the last deep levels before reset.
  - 2026-03-12 04:55 UTC: a new packet-fullness check suggests the better bounded frontier is probably depth `4`, not depth `5`, if this family is revived. After four rounds, a `128`-value wave has about `8.93` full 8-value depth-4 packets on average, but still zero homogeneous original blocks, so any packet/treelet phase still needs a real scatter/restore. By the next frontier after five rounds, depth-5 buckets have already fragmented to only about `4` values per non-empty bucket on average.
- Better overlap on the remaining gathered rounds, but only as a secondary optimization because it cannot bridge the whole gap to `1149` by itself.
  - 2026-03-11 05:00 UTC: explicit sweeps over wave width (`16..23`) and more aggressive critical-path scheduling on the original `2425` branch did not move the number at all. The gather line is load-bound, not scheduler-bound.
  - 2026-03-11 06:13 UTC: a corrected bucket-split viability study ruled out the most tempting regrouping variants before full implementation. Real prefix statistics on 400 random seeds were:
    - split after 2 branch bits: mean max bucket `72.6`, mean mixed boundary blocks `2.6`, mean homogeneous full blocks `29.4 / 32`
    - split after 3 branch bits: mean max bucket `40.3`, mean mixed boundary blocks `6.2`, mean homogeneous full blocks `25.9 / 32`
    - split after 4 or 5 bits: too fragmented to justify the reshuffle cost
  - 2026-03-11 06:13 UTC: corrected round-cost probes on the current kernel show why even the 2-bit/3-bit bucket splits are weak. With setup/store isolated by a `rounds=0` run, the current `256`-wide kernel costs about `70` cycles for depth `0`, `118` for the 2-way table round, `160` for the current 4-way table round, and `154..160` for generic gathered rounds. That means a 2-bit split only has one clearly cheaper replacement round (`118` versus `160`), while the current 4-way table is already basically as expensive as a gather round.
  - 2026-03-11 06:13 UTC: a one-shot proxy harness on smaller batch widths confirmed that per-bucket sequential execution is not viable. On the same height-10 tree, a `64`-wide kernel costs `91` cycles with `0` rounds, then `109/140/181/226/271/316` cycles for `1..6` rounds. Four such bucket-local passes are already slower than the current unified `256`-wide kernel before paying any bucket scatter / final restore cost.
  - 2026-03-11 07:48 UTC: this family is no longer just a theory note. The earlier “scheduler polish does nothing” conclusion only applied to the old per-round kernel emission. Once the full 16-round sequence was emitted as one DAG, a simple group-first scheduler cut the independent branch from `2285` to `1815` by letting earlier blocks keep advancing while later blocks were still filling their loads.
  - 2026-03-11 08:08 UTC: a follow-up sweep on that full-wave DAG found one more real overlap lever: a wider active wave is now helpful once some shallow-table work moves off `valu`. The best current point is `18` active groups; narrower waves lose overlap, while wider waves raise scratch pressure without paying it back.
  - 2026-03-11 08:58 UTC: another targeted scheduler search confirmed there is still small real headroom inside the full-wave DAG even after the `18`-group retune. The best measured point so far is: on `valu` only, frontload `stage < 7` and drain higher-numbered groups first. That buys `11` more cycles (`1689 -> 1678`). Nearby variants, load-order changes, flow-order changes, and wave-width retunes did not beat that point.
  - 2026-03-11 08:58 UTC: the next simple software-pipeline variant is now ruled out. Carrying precomputed next-round gathered addresses across the DAG stayed correct but regressed badly: full version `1775`, depth-2-only `1723`, depth-3+-only `1754`, depth-2/3-only `1731`. That means later-round deep-load overlap has to remove load work, not just hand later addresses across earlier.
  - 2026-03-11 19:45 UTC: the new depth-3-cached branch also closed out the next easy-looking overlap tweaks. A wider scheduler sweep on the valu-bound `1671` kernel found no better ready-op policy, widening the active wave back to `17` groups regressed to `1705`, and a pre-update-bit routing rewrite that reduced a few shallow valu ops still regressed to `1698` because it lengthened the actual schedule. That leaves the next credible step as another true deep-node-feeding reduction, not a shallow routing polish pass.
  - 2026-03-11 20:15 UTC: after the `1622` scheduler update, the local overlap story is even tighter. A broader engine-specific sweep found no point below `1622`, and the split-phase shallow/deep prototype lost badly even though it widened the deep phase. That makes the next credible step even more clearly a true deep-round node-feeding reduction rather than another scheduler variant.
  - 2026-03-11 21:54 UTC: the packed-setup rewrite materially changed the fixed overhead but did not change the round-body conclusion. The fused round core is still `1502` cycles, and even the newly legal `17`-group point still regressed to `1594`, so the next credible gain is still a deep-node-feeding reduction rather than more setup packing or another wave-width retune.
- Block-local unique-address dedup on the earliest gathered rounds.
  - 2026-03-11 06:13 UTC: an additional Athena review suggested a representative-lane dedup only for the first gathered rounds (especially depth `3`), trading repeated per-lane loads for scalar ALU copies. The useful conclusion is the scale, not the exact mechanism: even the optimistic upside looks more like `~100` cycles than `~1000`, so this family may still be worth a small pilot but not as a likely direct path to beating `1149`.
  - 2026-03-11 07:24 UTC: a second Athena review, after the shallow-wave fusion landed, reinforced the same conclusion from a different angle. On the current path-state architecture, even perfect shallow cleanup only moves the fixed/setup side and the first two table rounds; any remaining original win large enough to matter has to attack the deep gathered rounds directly (for example overlap/prefetch on the gather line or early repeated-address dedup), not the shallow prefix again.

## Additional constraints learned during the rewrite

- The current `build_mem_image()` layout does not leave any usable tail slack after the input values slice assignment, so the only safe runtime workspace in the submission harness is the reclaimed `inp_indices` plus `inp_values` regions (`512` words total once values are scratch-resident).
- A shallow-table or shallow-cache scheme can improve specific rounds, but with only `512` words of runtime workspace it cannot scale far enough beyond depths `1` and `2` to threaten the upstream `1149` result.
- `do_kernel_test()` in `perf_takehome.py` is only a valid incremental checker for paused scalar kernels. Vector kernels run to completion on the first `machine.run()`, so smaller-shape proxy measurements must use a one-shot submission-style harness instead of that helper.
- The current `1544` branch now uses about `1334 / 1536` words of scratch because path state is wave-local rather than block-global. That is enough to make even `20`-group waves legal again, but not enough to make them fast (`17..20 -> 1592 / 1613 / 1612 / 1604`), and it is still not enough to stage a naïve depth-4 cache plus a fresh per-group helper vector without giving the headroom back. Reusing the scratch window for a wider deep-only phase was not enough by itself; the earlier split-phase prototype still regressed to `2119`.
- The current `1501` branch still uses only about `1336 / 1536` words of scratch, so there is modest room for deeper pilots. The low-risk overlap line is narrower rather than completely dead: two successive exact-shape scheduler searches found real gains (`1537 -> 1508 -> 1501`), but the nearby threshold, stage-order, and non-`valu` engine variants now flatten again at `1501`, and the larger structural negatives still stand. The remaining direction with plausible tens-of-cycles upside is still bounded depth-`4..6` regrouping / node-major execution rather than another generic wave-size or shallow-cache tweak.
